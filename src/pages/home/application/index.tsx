import { memo, useEffect } from 'react';
import './index.less';

const Application = memo(() => {
  useEffect(() => {}, []);
  return <section className='Application'>Application</section>;
});
export default Application;
