import { memo, useEffect } from 'react';
import './index.less';

const Opening = memo(() => {
  useEffect(() => {}, []);
  return <section className='Opening'>opening</section>;
});
export default Opening;
